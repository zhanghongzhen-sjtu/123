# Model Stage Confirmation

MODEL_STAGE_CONFIRMED_BY_USER

The user confirmed entering the RTX 5090 model stage.

Allowed now:

- install lightweight OpenVLA-OFT Python dependencies if needed
- import OpenVLA-OFT modules
- run UAV constants checks
- run action head / proprio projector shape-only checks
- prepare minimal processor/model smoke plans

Still not allowed without separate confirmation:

- full OpenVLA-OFT training
- LoRA/OFT training
- closed-loop simulation
- full TravelUAV training
